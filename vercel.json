{
  "version": 2,
  "buildCommand": "npm install && npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/inngest.js": {
      "includeFiles": "node_modules/**"
    }
  },
  "rewrites": [
    { "source": "/api/inngest", "destination": "/api/inngest" }
  ]
}